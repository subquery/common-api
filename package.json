{
  "name": "commonApi",
  "version": "1.0.0",
  "description": "A subquery project provide common api for a substrate based network",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/common-api",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "jay ji",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^4.16.2",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}
